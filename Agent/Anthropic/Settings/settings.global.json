{
  "disableBypassPermissionsMode": true,
  "permissions": {
    "ask": [
      "git reset --hard*",
      "git clean -fd*",
      "rm -rf*",
      "del /f /q*",
      "chmod -R*",
      "pip install*",
      "pip3 install*"
    ]
  }
}
